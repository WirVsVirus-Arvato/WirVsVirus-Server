spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/corona-app
    username: root
    password: pw

  flyway:
    url: jdbc:postgresql://localhost:5432/corona-app
    user: root
    password: pw

server:
  servlet:
    context-path: /api